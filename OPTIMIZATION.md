# 找货优化与验收说明

本次优化针对“同品牌但规格、口味、包装不对”，覆盖目标识别、召回、逐 SKU 核验、图片理解、价格库存、任务恢复和结果交付。真实 SKU 准确率与线上耗时由人工验收决定，离线回归不替代平台验收。

## 已实现

| 环节 | 当前行为 |
|---|---|
| 模型 | 文本和商品图片均默认 `qwen3.8-flash`，温度 0、关闭思考、结构化 JSON，记录耗时和 token |
| 工作流 | 带原始任务的找货请求进入固定流程，减少通用 Agent 往返和重复导出 |
| 目标 | 校验商品 ID，收集全部已选规格；只读取 ID 一致的页面状态/JSON-LD；静态网页只补空字段 |
| 匹配 | 同一 SKU 行核对品牌、品类、系列、口味、颜色、形态、单件净含量、件数、包装层级、型号及纸品参数；不跨行拼条件 |
| 单位包装 | 区分单件含量、包装件数、包装层级、销售单位和赠品；同维度质量/容积换算，纸品尺寸统一毫米；明确允许包装替代才折算采购量 |
| 图片 | 主图有商品来源，附图要求 SKU 绑定；同图只提取一次可见事实，各 SKU 独立比较；图文冲突否决，模糊图不能借文字通过；缺证据按需追加标签/规格图 |
| 检索 | 多组文搜加图搜；每轮 8–12 个详情，按供应商分配名额，合格不足继续查候选、补副来源、改词和扩页；默认总详情预算 80，默认并发 3 |
| 价格库存 | 绑定匹配行的 SKU 价、阶梯价上下限、MOQ、库存和采购量，识别报价/销售单位；只刷新最终 6 家及 2 家备选的过期详情，受剩余预算限制 |
| 采购成本 | 区分商品金额、整单运费、额外税费、已确认优惠和到货总成本；报价绑定 SKU、采购量、收货地区；未知项留空 |
| 缓存 | 全参数 SHA-256、接口账号隔离、原子写入、并发相同请求合并；搜索 5 分钟、详情默认 60 秒、店铺 1 小时；图片 URL 缓存默认 1 小时，OCR 按内容、模型和提示词版本缓存 24 小时 |
| 排序 | 先区分同款和可采购；已确认可采购优先，再综合到货成本与服务；库存或成本未知的同款保留提醒；供应商去重 |
| 容错 | 单候选/通道失败不阻断其他项；阶段到期停止等待，迟到结果不能合并或写缓存；任务结束取消剩余请求并关闭复用连接 |
| 条码辅助 | 收集目标和 SKU 的 API 条码及清晰标签数字，校验位有效且同包装层级才能比较；冲突待确认，缺码不拒绝，不能单靠码放行 |
| 反馈评估 | 每次导出空白人工反馈表和冻结快照；导入明确判断，回放规则并记录原始排名质量；未验证项不当作正确或错误 |
| 调度 | 原文、采购要求、队列、来源会话和暂停状态持久化；失败重启保留原始任务、恢复未过期阶段；停止仅取消指定 worker |
| 会话 | 群聊上下文按用户隔离；明确状态/帮助/停止命令跳过模型 |
| 输出 | 保留原 13 列和 B9 摘要，追加 SKU、图文证据、采购状态、成本明细和条码核验；成本完整且京东含税价明确才显示利润率 |
| 交付 | manifest 校验 CSV 哈希、表头、合格行数；上传失败重试并记录交付状态 |
| 部署 | 冻结 Python 依赖，固定兼容 TCP 桥接的 OICC revision；双平台共用补丁校验；Windows 启动只处理本项目进程 |

## 模型配置

私有密钥只放被 Git 忽略的 `config.yaml`。文本与视觉两个角色均配置：

```yaml
base_url: https://dashscope.aliyuncs.com/compatible-mode/v1
model: qwen3.8-flash
api_key: <与服务地址匹配的有效密钥>
temperature: 0
max_tokens: 8192
extra_body:
  enable_thinking: false
```

参数依据[阿里云 Chat API](https://help.aliyun.com/zh/model-studio/qwen-api-via-openai-chat-completions)和[视觉理解文档](https://help.aliyun.com/en/model-studio/vision)。服务地址必须与密钥匹配；401 表示认证未通过，不能通过改模型名称解决。

`ecom_best_source.runtime` 控制并发、阶段时限和缓存。默认总任务时限 600 秒，补搜时限 480 秒；示例配置为总图片核验 24 个 SKU、并发 3、单次模型时限 45 秒。已有 `vision.max_pairs` 在多轮之间共享，不随补搜轮数倍增；`runtime.vision_max_pairs` 可显式覆盖它。两项都省略时使用 60 个 SKU 的总预算。单个 SKU 的补图/矛盾响应复核可能产生多次模型请求，实际调用另记在 metrics。

默认启用图文联合核验。SKU 文字已经明确的整箱数量不要求单罐图重复证明，但图片必须提供有效同款证据，图文明确冲突不能通过。关闭 `vision.enabled` 仅适合对照实验，此时没有图片核验保障。

`vision.image_cache_seconds` 默认 3600，填 0 可禁用跨任务图片 URL 缓存；OCR 仍按图片内容哈希缓存，不跨 SKU 复用匹配结论。任务内复用 HTTP/API 会话，缓存命中和并发请求合并分别计数。`onebound.cost_per_call_yuan` 仅填写账号实际单次价格才生成估计，留空表示未知，不代表免费。

## 2026-09-07 高优先级修复

- `100g×6袋（单袋100g）`、`单瓶500ml×24瓶` 保留整包数量；纸品的 `24包` 不再进入净含量字段。
- 统一品牌/品类别名、尺寸单位、开放口味抽取，糖分与脂肪独立比较；SKU 属性来源和冲突随选中结果保存。
- 静态京东页面仅使用目标 SKU JSON-LD、商品页面元数据及商品图区域；排除推荐区与占位图。回退页的标题和图片整体选取，避免跨页面拼接。
- 图片比较按供应商轮流分配额度，未覆盖、无图、识别失败、证据不足分别记录；同证据已完成的核验不会每轮重跑。
- 找到不足 6 家时按预算继续检索；记录详情/图片使用量、未核对候选和停止原因，区分找到足够货源、预算耗尽和已搜索范围结束。

## 2026-09-07 后续优化

- 京东页面的必需字段已经稳定时提前结束轮询；可选价格缺失不再固定等满所有轮次，多 SKU 尚未绑定时继续等待。
- 任务、检索、详情与重试共享剩余时间；超时和取消隔离迟到结果，候选之间互不阻塞。最终刷新保留身份一致的图片/条码证据，源 SKU、图片或原始条码改变则重新核验。
- 运费必须有对应采购量、地区和 SKU 的报价依据；同省不同市/区不能误用明确绑定报价。免邮与优惠先验证适用门槛，已含税价格不再重复加税。
- 单位不明时不臆造库存或 MOQ 换算；缺失库存、税费或运费的同款仍可展示为“可购性待确认”，已知无库存、起批量不满足或无法配送的结果不推荐。
- 条码读取清晰完整的数字，只比较明确标为 `unit` 或 `case` 的同层级标识；不按码长猜层级。校验算法参考 [GS1 校验位规则](https://www.gs1.org/services/how-calculate-check-digit-manually)，包装数量涉及独立编码，参见 [GS1 包装数量规则](https://www.gs1.org/1/gtinrules/en/rule/270/packcase-quantity)。有效码不证明真伪，也不能替代规格、口味与包装核验。
- 生成轻量反馈 CSV，按目标与 offer/SKU 固定判定 ID；重复导出保留已填写内容，冻结快照被改动时拒绝导入。无需新增 Web 平台或数据库。

## Windows 运行

```powershell
Set-Location D:\ai\all-in-ai\hermes-chrome
.\setup.ps1
# 按 deploy/README.md 安装所需浏览器实例并注册扩展 ID。
# 按本机实际路径配置 config.yaml 中 browsers 与 skills.dir。
.\start.ps1 -DryRun
.\start.ps1
```

在已配置机器人中发送“b1 找货 <京东链接>，只要草莓味100g×6袋，采购3件，不接受包装替代”。也可独立运行：

```powershell
.\.venv\Scripts\python.exe -m agent.worker --worker-id b1 --skill ecom-best-source --task "找货 <京东链接>，草莓味100g×6袋，采购3件"
```

结果在 `outputs/<任务目录>/`。浏览器不可用会尝试静态资料补全；缺京东价时利润率留空，关键 SKU 证据不足时待确认。

## 人工 SKU 验收

建议用 20–50 个有标准答案的商品，覆盖不同口味、重量、袋数、包装、多 SKU 共图、图文冲突和阶梯价。每次固定京东 SKU 与采购数量。

1. 检查 CSV 的目标规格是否对应已选 SKU 和本次要求。
2. 点开候选详情，选择 CSV 指定 SKU，核对口味、单件含量、件数、包装和图片。
3. 按相同采购量核对价格、MOQ、SKU 库存；平台数据可能随时间变化。
4. 记录错货数、漏掉的真实同款数和待确认比例；待确认不能计作自动匹配正确。
5. 查看 `.ecom-scratch/metrics.json` 阶段耗时、接口错误、模型用量；规则耗时不能代替完整任务耗时。

库存“待确认”不表示现货；未知库存保留为空而非 0。“总进货价(元)”与“到货总成本(元)”现在表示已核实的商品金额加整单运费、额外税费减已确认优惠；任何必需项未知就留空，可在“商品金额(元)”查看已有价格信息。利润率还要求目标京东售价含税口径明确，不包括销售平台费用。用户未给数量时沿用页面明确数量或 1，CSV 展示实际采购 SKU 数量。

每份结果会附 `*-反馈.csv` 与同名 `.snapshot.json`。只填写“人工判断”（正确/错误/待确认）、“错配原因”、“可采购”（可采购/不可采购/待确认）和“备注”，保留判定 ID 与快照。空白和待确认不产生准确率标签。填写后运行：

```powershell
.\.venv\Scripts\python.exe skills/ecom-best-source/scripts/sourcing_feedback.py import "outputs/<任务目录>/<结果名>-反馈.csv"
.\.venv\Scripts\python.exe -m scripts.evaluate_sourcing "outputs/<任务目录>/<结果名>-反馈.labels.json" --output "outputs/<任务目录>/人工评估.json"
```

反馈数据作为下一次改规则的回归输入；不会自动训练模型或把系统推荐伪装成人工正确答案。只有实际核对并标注过的商品参与对应分母。指标汇总默认显示原始推荐的人工判定结果，同时展示审核覆盖率；当前规则重跑单列为 `rule_replay`，避免规则改好后覆盖历史错配率。

## 离线回归与指标

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m scripts.evaluate_sourcing tests/fixtures/sourcing-labels.json --output outputs/offline-evaluation.json
.\.venv\Scripts\python.exe -m scripts.sourcing_metrics --outputs outputs
```

普通 pytest 默认禁止找货接口访问，并跳过真实模型/浏览器测试；只有显式 `--run-live` 才启用原有外部连通性测试。新增回归含 100 组“口味×重量×件数”及各组近似错货反例，完整流程模拟覆盖正确匹配、错口味、图片超时、详情失败。

人工标注格式见 `tests/fixtures/sourcing-labels.json`：每个 case 保存完整 target、带 SKU 详情/视觉证据的 candidates、应输出的 expected_matches（offer ID + SKU ID）。该文件是合成回归示例，不是真实 SKU 结果。完整标签的无同款用空列表；反馈导入的部分标签同时保存 `labels_complete=false` 和明确 `judgments`。回放按真实推荐上限（默认 6 家）计算 precision/recall、首条准确率与前三条命中率，另列已评分样本数、未验证推荐数和 `observed_ranking` 原始排名指标；不重新调用搜索或模型。

指标脚本汇总已有任务 P50/P95、阶段耗时、图片调用、缓存命中和 token，并按模型/品类拆分，展示召回、详情、图片、推荐及已确认可采购数量。API 请求使用任务最终累计值，避免多轮重复计数；配置费用只是估计，不是账单。没有明确人工标注时准确率保持未知。

机器人实际消息收发、浏览器登录态、账号权限、真实找货准确率和速度仍需实际环境验收，本次不将模拟结果声明为真实 SKU 准确率。
